/* A program to compute the factorial value of an input */
/* The errors contained in this test file are
    function declaration with no parameters and without a body
    variable declaration without a recognized type
*/

void main(void) {
  int x;
  int fac;
  double y;

  x = input();
  fac = 1;

  while (x > 1) {
    fac = fac * x;
    x = x - 1;
  } 

  output(fac);
}

int test2(void)